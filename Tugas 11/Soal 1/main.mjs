// main.js

// Impor pi, add, dan subtract dari math.js
import { pi, add, subtract } from "./math.mjs"; 

// Tampilkan nilai pi ke konsol
console.log(`Nilai pi: ${pi}`);

// Tampilkan hasil dari add(5, 3) ke konsol
console.log(`Hasil add(5, 3): ${add(5, 3)}`);

// Tampilkan hasil dari subtract(10, 4) ke konsol
console.log(`Hasil subtract(10, 4): ${subtract(10, 4)}`);
